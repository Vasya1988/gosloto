
const getNumber = (props: any) => {
    
    const getCell = document.body.querySelectorAll('[data-selected="true"]') as NodeListOf<HTMLElement>
    // Array.from(getCell).map((item) => {
        
    // })
    console.log('dd')
}

export { getNumber };